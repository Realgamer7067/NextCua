"""
dataset — Record user sessions and process them into ML training data.

Modules:
    recoder          Record keyboard/mouse/touchpad events + screen video.
    data_processing  Enrich recorded sessions with OCR + object detection.
"""
